# 記憶統合タスク（週次）

{anima_name}、1週間分の記憶を整理する時間です。

## 現在の知識ファイル（全{total_knowledge_count}件）

{knowledge_files_list}

## マージ候補（類似ファイルペア）

{merge_candidates}

## 重要な制約
- **この作業はあなた自身が直接実行すること（MUST）**。`delegate_task`、`submit_tasks`、`send_message` は使用禁止。部下への委任やメッセージ送信は一切行わず、記憶操作ツールのみで作業を完結させること

## 作業手順

### 1. マージ候補の統合（最優先・MUST）
上記のマージ候補は、RAGベクトル類似度に基づいて検出された統合すべきファイルペアです。
**すべてのペアについて** 以下を実施してください:
1. `read_memory_file` で両方のファイル内容を確認
2. 内容を統合して `write_memory_file` で書き込み
3. 不要になった方を `archive_memory_file` でアーカイブ
4. `[IMPORTANT]` タグがある場合は統合先にも必ず残す

マージ候補がない場合はこのステップをスキップ。

### 2. 追加の重複チェック
上記のファイル一覧を確認し、マージ候補に含まれていなくても類似するファイルがあれば:
- `read_memory_file` で内容を確認
- 統合すべきなら同様の手順でマージ

### 3. [IMPORTANT] 知識の概念昇華
作成から30日以上経過した `[IMPORTANT]` 付き knowledge/ ファイルを概念統合する。具体的な出来事の記録を、普遍的な原則・ルールに昇華させる作業。

1. `search_memory` で `[IMPORTANT]` を含む knowledge/ を検索し、30日以上前のものを `read_memory_file` で確認
2. 関連テーマでグループ化し、各グループから抽象的な原則を抽出
3. `concept-{テーマ}.md` として `write_memory_file` で作成（本文先頭に `[IMPORTANT]` 付与）
4. 元ファイルから `[IMPORTANT]` タグを除去（ファイル自体は残す。自然に忘却される）

孤立した `[IMPORTANT]`（関連グループなし）や30日未満のものはスキップ。既に概念レベルのものは再統合不要。

### 4. 手続き知識の整理
`Glob` で procedures/ 内のファイルを確認し:
- 古くなった手順 → 現状に合わせて更新するか、`archive_memory_file` でアーカイブ
- 使われていない手順 → アーカイブを検討
- 類似の手順 → 統合

### 5. 古いエピソードの圧縮
`Glob` で episodes/ を確認し、30日以上前のファイルがあれば:
- `read_memory_file` で内容を確認
- [IMPORTANT] タグがないものは要点のみに圧縮して `write_memory_file` で上書き

### 6. 知識の矛盾解消
矛盾する知識ファイルがないか確認し:
- 最新の情報に基づいて正確な方を残す
- 古い方は `archive_memory_file` でアーカイブ

完了後、実施した内容のサマリーを出力してください。
