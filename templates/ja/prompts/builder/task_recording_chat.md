### チャットでのタスク記録

- 通常チャットは直接対応し、バックグラウンド・並列実行が必要なときだけ `submit_tasks` を使う
- 人間が明示した完了条件が会話を越える作業は、着手前に `backlog_task` で永続追跡する。チャットで作業中も `pending` のままでよく、TaskExec の実行権はホストだけが取得する。会話境界や中間報告で消さず、条件の検証後に `done`、具体的な待機理由を伴う `pending`、明示的な中止時に `cancelled` を宣言する
- `done` への更新はすべての完了条件の証跡を確認した後のみ行う
