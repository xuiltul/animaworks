### Heartbeat でのタスク記録

- 既存タスクと要対応理由を確認する。中断した未終了タスクは原因解消後に限り、`submit_tasks` に既存 task_id と `resume: true` を指定して再開する。pending の一括投入や保存済み入力の再構築はしない。承認済みの範囲を守り、すべての完了条件の証跡を確認した後だけ `done` にする
- Anima間の委任もタスクキューに記録し、relay_chainを更新せよ
- タスク完了時は `update_task` でステータスを更新せよ
