#!/usr/bin/env bash
# send — Person間メッセージ送信ラッパー
# Usage: bash send <recipient> "message" [--reply-to ID] [--thread-id ID]
set -euo pipefail
SELF="$(basename "$(pwd)")"
MAIN="${ANIMAWORKS_PROJECT_DIR:-/home/main/dev/animaworks}/main.py"
exec python3 "$MAIN" send "$SELF" "$@"
