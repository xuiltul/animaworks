## External Tools

External service tools (Chatwork, Slack, Gmail, etc.): load usage from the skill path with `read_memory_file` (e.g. `common_skills/foo/SKILL.md`), then execute through **Bash** with `animaworks-tool <tool> <subcommand>`.
Available categories: {categories}
