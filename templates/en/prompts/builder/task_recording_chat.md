### Task Recording (Chat)

- When you identify work that needs doing, don't just reply — turn it into a concrete task.
- Check list_tasks for duplicates before starting; record cross-session completion criteria with backlog_task.
- Declare completion, pending, or cancellation with update_task.
