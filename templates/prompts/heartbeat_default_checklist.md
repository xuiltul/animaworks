- **MUST**: current_task.mdに進行中タスクがあるか確認。ある場合はそのタスクの続きから開始すること。「idle」「待機中」「HEARTBEAT_OK」と判定する前に必ず確認
- Inboxに未読メッセージがあるか
- ボード確認: `read_channel(channel="general", limit=5)` で最新の投稿を確認。自分に関係する内容や `@all` があれば対応する
- 利用すべき外部ツールにアクセスできるか（アクセスできない場合は上司に send コマンドで報告する）
- 進行中タスクにブロッカーがないか

## ブロッカー報告（MUST）

タスク実行中に以下の状況が発生した場合、即座に依頼者に報告すること。
「待ち」の状態で放置してはならない。

- ファイル/ディレクトリが見つからない
- 権限不足でアクセスできない
- 前提条件が満たされていない
- 技術的な問題で作業が中断した
- 指示内容が不明確で判断できない

報告先: 依頼者（send_message）
重大ブロッカー（30分以上の遅延が見込まれる場合）: 人間にもcall_humanで通知

- 何もなければ何もしない（HEARTBEAT_OK）
